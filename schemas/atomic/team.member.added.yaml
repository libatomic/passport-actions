$schema: https://json-schema.org/draft/2020-12/schema
$id: libatomic/passport-actions/schemas/atomic/team.member.added
version: "1.0.0"
title: team.member.added
description: >
  Event body schema for team.member.added. Contains the team membership
  details for the added user.

type: object
properties:
  team_id:
    type: string
    format: uuid
  role:
    type: string
  invited_by:
    type: string
    format: uuid
    description: The user who added this member
