$schema: https://json-schema.org/draft/2020-12/schema
$id: libatomic/passport-actions/schemas/atomic/team.member.added
version: "1.0.0"
title: team.member.added
description: >
  Placeholder for team member additions. This event type is defined but
  not currently emitted from application code.

type: object
properties:
  team_id:
    type: string
    format: uuid
  user_id:
    type: string
    format: uuid
  role:
    type: string
  invited_by:
    type: string
    format: uuid
    description: The user who added this member
