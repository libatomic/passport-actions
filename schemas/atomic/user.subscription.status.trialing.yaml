$schema: https://json-schema.org/draft/2020-12/schema
$id: libatomic/passport-actions/schemas/atomic/user.subscription.status.trialing
version: "1.0.0"
title: user.subscription.status.trialing
description: >
  Emitted when a subscription transitions to trialing status.

type: object
required:
  - status
  - subscription_id
properties:
  prev_status:
    type: string
    description: Previous subscription status
  status:
    type: string
    description: New subscription status
  subscription_id:
    type: string
    format: uuid
    description: Subscription ID
  stripe_subscription:
    type: string
    description: Stripe subscription ID (when applicable)
