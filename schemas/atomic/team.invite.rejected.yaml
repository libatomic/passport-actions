$schema: https://json-schema.org/draft/2020-12/schema
$id: libatomic/passport-actions/schemas/atomic/team.invite.rejected
version: "1.0.0"
title: team.invite.rejected
description: >
  Emitted when a team (entitlement) invite is rejected. Body is the
  serialized CreditInviteCancelInput.

type: object
properties:
  invite_code:
    type: string
  status:
    type: string
