$schema: https://json-schema.org/draft/2020-12/schema
$id: libatomic/passport-actions/schemas/atomic/gift.invite.rejected
version: "1.0.0"
title: gift.invite.rejected
description: >
  Emitted when a gift invite is rejected. Body is the serialized
  CreditInviteCancelInput.

type: object
properties:
  invite_code:
    type: string
  status:
    type: string
