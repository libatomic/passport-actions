$schema: https://json-schema.org/draft/2020-12/schema
$id: libatomic/passport-actions/schemas/atomic/credit.deleted
version: "1.0.0"
title: credit.deleted
description: >
  Emitted when a credit is deleted. Body is the serialized
  CreditDeleteInput.

type: object
required:
  - credit_id
properties:
  owner_id:
    type: string
    format: uuid
  credit_id:
    type: string
    format: uuid
