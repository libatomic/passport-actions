$schema: https://json-schema.org/draft/2020-12/schema
$id: libatomic/passport-actions/schemas/atomic/user.role.added
version: "1.0.0"
title: user.role.added
description: >
  Emitted when roles are added to a user who already has existing roles.

type: object
required:
  - roles
  - user
properties:
  roles:
    type: array
    items:
      type: string
  user:
    $ref: libatomic/passport-actions/schemas/atomic/user@v1.0.0
