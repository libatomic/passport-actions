$schema: https://json-schema.org/draft/2020-12/schema
$id: libatomic/passport-actions/schemas/atomic/user.auth.success
version: "1.0.0"
title: user.auth.success
description: >
  Emitted when a user authenticates successfully.

type: object
required:
  - login
properties:
  login:
    type: string
