$schema: https://json-schema.org/draft/2020-12/schema
$id: libatomic/passport-actions/schemas/atomic/user.email.bounced
version: "1.0.0"
title: user.email.bounced
description: >
  Emitted when a user's email is returned due to a bounce or complaint.

type: object
properties:
  email:
    type: string
    format: email
  type:
    type: string
  reason:
    type: string
