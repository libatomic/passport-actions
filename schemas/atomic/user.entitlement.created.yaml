$schema: https://json-schema.org/draft/2020-12/schema
$id: libatomic/passport-actions/schemas/atomic/user.entitlement.created
version: "1.0.0"
title: user.entitlement.created
description: >
  Emitted when an entitlement is created. Body is the serialized
  Entitlement struct (excluding user, subscription, audiences).

$ref: libatomic/passport-actions/schemas/atomic/entitlement@v1.0.0
