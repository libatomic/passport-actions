$schema: https://json-schema.org/draft/2020-12/schema
$id: libatomic/passport-actions/schemas/atomic/user.role.removed
version: "1.0.0"
title: user.role.removed
description: >
  Emitted when roles are removed from a user who still has remaining roles.

type: object
required:
  - roles
  - user
properties:
  roles:
    type: array
    items:
      type: string
  user:
    $ref: libatomic/passport-actions/schemas/atomic/user@v1.0.0
