$schema: https://json-schema.org/draft/2020-12/schema
$id: libatomic/passport-actions/schemas/atomic/gift.invite.redeemed
version: "1.0.0"
title: gift.invite.redeemed
description: >
  Emitted when a gift invite is redeemed. Body is the serialized
  CreditInviteAcceptInput.

type: object
properties:
  user_id:
    type: string
    format: uuid
  invite_code:
    type: string
