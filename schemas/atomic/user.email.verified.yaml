$schema: https://json-schema.org/draft/2020-12/schema
$id: libatomic/passport-actions/schemas/atomic/user.email.verified
version: "1.0.0"
title: user.email.verified
description: >
  Emitted when a user's email is confirmed. Contains the member profile and
  instance context.

type: object
required:
  - member
  - instance
properties:
  member:
    $ref: libatomic/passport-actions/schemas/oauth/openid/profile@v1.0.0
  instance:
    $ref: libatomic/passport-actions/schemas/atomic/instance@v1.0.0
