$schema: https://json-schema.org/draft/2020-12/schema
$id: libatomic/passport-actions/schemas/atomic/user.entitlement.canceled
version: "1.0.0"
title: user.entitlement.canceled
description: >
  Emitted when an entitlement is canceled. Same Entitlement struct as
  user.entitlement.created.

$ref: libatomic/passport-actions/schemas/atomic/entitlement@v1.0.0
