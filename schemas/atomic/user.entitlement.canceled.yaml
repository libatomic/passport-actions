$schema: https://json-schema.org/draft/2020-12/schema
$id: libatomic/passport-actions/schemas/atomic/user.entitlement.canceled
version: "1.0.0"
title: user.entitlement.canceled
description: >
  Event body schema for user.entitlement.canceled. Contains the canceled
  entitlement object.

type: object
properties:
  id:
    type: string
    format: uuid
  plan_id:
    type: string
  status:
    type: string
  begins_at:
    type: string
    format: date-time
  ends_at:
    type: string
    format: date-time
  quantity:
    type: integer
  recurring_interval:
    type: string
  recurring_frequency:
    type: integer
