name: password-reset-sms
description: |
  Send a password reset code via SMS.
  Replaces the template trigger for event: user.password.reset.sms
category: authentication
replaces:
  event_type: user.password.reset.sms
  channel: sms
inputs:
  template:
    description: The SMS template name
    default: password-reset-sms
definition:
  name: password-reset-sms
  version: 1

  on:
    - event: user.password.reset.sms

  steps:
    - id: load-user
      action: user.get
      with:
        id: ${{ trigger.user_id }}

    - id: send-code
      action: sendsms
      with:
        to: ${{ steps.load-user.outputs.user.profile.phone_number }}
        template: ${{ inputs.template }}
        data:
          code: ${{ trigger.body.code }}
